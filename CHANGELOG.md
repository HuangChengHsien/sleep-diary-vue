# 變更紀錄

這份文件**只記錄會改變顯示數字、或改變數據判讀方式的變更**。

重構、拆檔、修 lint、加測試等不影響輸出的改動不會出現在這裡 —— 那些請看 git 紀錄與 Pull Request 說明。

會這樣區分是因為這個工具產出的報告會列印出來、拿到門診討論。如果日後有人問「這份報告上的數字是怎麼算的」，這份文件加上報告頁尾的「程式版本」戳記，是唯一能回答的依據。

日期即版本；報告表頭印出的「程式版本」就是建置日期，可直接對照下方條目。

---

## 2026-07-31

### 每日平均總睡眠的計算方式改變

原本以「入睡當下的日曆日」分桶，就寢時間跨過午夜時，同一晚會被算成不同天，兩晚也可能被併進同一天。改為以早上 9 點為界的「邏輯日」，與夜晚平均就寢／入睡／起床時間使用同一套規則。

作息規律的個案數字不變。就寢時間漂移到凌晨的個案，此欄位數字會下降到接近真實值 —— 修正前兩個各 7.5 小時的夜晚會被合併顯示為 15.0 小時。

**此日期之前列印的報告，這個欄位可能與現在不同。**

### 圖表不再隱藏過長的入睡耗時

原本時間軸與入睡耗時趨勢圖會濾掉超過 180 分鐘的入睡耗時。這使得「躺三小時睡不著」這種最需要被看見的情況，與輸入錯誤一起被靜默丟棄，趨勢線看起來比實際狀況好。

現在一律照實呈現。**此日期之前列印的入睡耗時趨勢圖，可能缺少最嚴重的幾晚。**

180 分鐘這個門檻現在只用於存檔前的提醒，不再用於任何過濾。

### 統計卡新增資料揭露

- **平均要多久才會入睡**：當納入計算的夜晚中有超過 3 小時者，卡片下方會註明有幾晚，並說明平均值已被拉高、不代表典型狀況。此提醒只陳述事實，不判斷成因 —— 可能是輸入錯誤，也可能是真的入睡困難。
- **夜晚統計**：上床時間落在白天（09:00–18:00）但入睡時間在夜晚的紀錄，會整晚不計入四項夜晚統計。這是既有行為，現在會在統計卡下方明白說出來，並提示常見原因是輸入時上下午選錯。

### 新增「不列入分析」功能

可將個別紀錄或整段日期區間標記為不列入分析，用於生病、住院、旅行時差等「資料是真的、但不具代表性」的期間。

- 統計卡、每日明細表與五張趨勢圖**不含**被排除的紀錄
- 時間軸圖表**仍會以淡色畫出**被排除的紀錄，以保留前後文
- 分析頁與列印報告會列出被排除的期間
- 標記會隨備份匯出與還原一併保存

### 存檔前的輸入提醒

新增睡眠紀錄或編輯時，若上床到入睡的間隔超過 3 小時，儲存前會跳出確認。只提醒、不阻擋 —— 極端個案確實可能出現超長的入睡耗時。

常見成因是時間選擇器的上下午選錯（例如 22:55 誤選成 12:55）。

### 分析頁修正

切換到沒有記錄的個案，或篩選範圍內沒有資料時，畫面會清空並說明原因。修正前會留著**上一個個案的圖表**，且沒有任何提示。

### 列印報告新增表頭

列印時會加上個案姓名、統計期間、顯示範圍、列印日期與程式版本。修正前，列印出的報告不含個案姓名。
